👉 **Différence :**

* `module.exports = fonction` → le module **exporte une seule fonction** (ou une seule valeur).
* `module.exports = { … }` → le module **exporte un objet** contenant **plusieurs fonctions ou valeurs**.

---

👉 **Quand les utiliser :**

* ✅ `module.exports = fonction` → quand le module n’a **qu’une seule responsabilité principale**.
  *Exemple :* une fonction utilitaire unique.
* ✅ `module.exports = { … }` → quand le module doit **fournir plusieurs fonctions ou constantes**.
  *Exemple :* une bibliothèque avec plusieurs méthodes.
